# Gym Dashboard

Static, mobile-friendly dashboard for a personal Push/Pull/Legs training log (stats, charts, trends). Built from data digitized in a separate private repo. `index.html` + `gym_data.js` only — no photos or raw files.
